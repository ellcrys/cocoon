syntax = "proto3";
package proto;

service Stub {
    rpc Invoke(Tx) returns (Tx);
}

message Tx {
    int32 status = 1;
    string Id = 2;
    string name = 3;
    repeated string params = 4;
    bytes body = 5;
}