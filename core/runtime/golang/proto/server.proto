syntax = "proto3";
package proto;

service Stub {
    rpc Invoke(InvokeParam) returns (InvokeResponse);
}

message InvokeParam {
    string ID = 1;
    string function = 2;
    repeated string params = 3;
}

message InvokeResponse {
    string ID = 1; 
    int32 status = 2;
    bytes body = 3;
}