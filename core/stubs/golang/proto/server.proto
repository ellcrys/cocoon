syntax = "proto3";
package proto;

service Stub {
    rpc Transact(stream Op) returns (stream Tx);
}

message Tx {
    string id = 1;
    string name = 2;
    string value = 3;
}

message Op {
    string name = 1;
    string value = 2;
}