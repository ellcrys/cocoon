syntax = "proto3";
package proto;

service Stub {
    rpc Transact(stream Resp) returns (stream Tx);
}

message Tx {
    string id = 1;
    string name = 2;
    repeated string params = 3;
}

message Resp {
    string name = 1;
    string value = 2;
}