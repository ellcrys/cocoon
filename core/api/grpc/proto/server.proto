syntax = "proto3";
package proto;

service API {
    rpc Cocoon(Request) returns (Response);
}

message Request {
    string name = 1;
    repeated string params = 2;
}

message Response {
    int32 status = 1;
    bytes body = 2;
}
